# Ruff config for the dist/ tree generated by `python -m tools.sync_to_core`.
#
# This is a snapshot of the ruff [tool.ruff*] sections from the Home
# Assistant core pyproject.toml — kept here so the sync script can lint
# its output the same way core's CI would, without polluting the custom
# repo's own (lighter) ruff config.
#
# The sync script copies this file into dist/ruff.toml before running
# `ruff check` / `ruff format` against the dist tree, so the
# per-file-ignores globs below resolve against that layout (with the
# `neopool/` prefix because the produced subtree is `dist/neopool/`).
#
# Source:  https://github.com/home-assistant/core/blob/dev/pyproject.toml
# Snapshot: 2026-06-12 (Python 3.14 era)
#
# To refresh:
#   1. Diff against home-assistant/core@dev/pyproject.toml
#   2. Copy the [tool.ruff*] sections verbatim, dropping the `tool.`
#      prefix (this file is loaded as a stand-alone ruff.toml).
#   3. Update the snapshot date above

required-version = ">=0.15.15"

# Pin to Python 3.14 so the formatter applies PEP 758 (drop redundant
# `except (X, Y):` parentheses). Core gets this implicitly from
# `requires-python = ">=3.14.2"` in its pyproject.toml; a stand-alone
# ruff.toml has no such hint, so without this line ruff defaults to
# its lowest supported target and re-introduces the parentheses on
# every sync run.
target-version = "py314"

[lint]
select = [
    "A001",   # Variable {name} is shadowing a Python builtin
    "ASYNC",  # flake8-async
    "B",      # flake8-bugbear
    "BLE",
    "C",      # complexity
    "COM818", # Trailing comma on bare tuple prohibited
    "D",      # docstrings
    "DTZ003", # Use datetime.now(tz=) instead of datetime.utcnow()
    "DTZ004", # Use datetime.fromtimestamp(ts, tz=) instead of datetime.utcfromtimestamp(ts)
    "DTZ011", # Use datetime.now(tz=).date() instead of date.today()
    "E",      # pycodestyle
    "F",      # pyflakes/autoflake
    "F541",   # f-string without any placeholders
    "FLY",    # flynt
    "FURB",   # refurb
    "G",      # flake8-logging-format
    "I",      # isort
    "INP",    # flake8-no-pep420
    "ISC",    # flake8-implicit-str-concat
    "ICN001", # import concentions; {name} should be imported as {asname}
    "LOG",    # flake8-logging
    "N804",   # First argument of a class method should be named cls
    "N805",   # First argument of a method should be named self
    "N806",   # Variable {name} in function should be snake_case
    "N815",   # Variable {name} in class scope should not be mixedCase
    "PERF",   # Perflint
    "PGH",    # pygrep-hooks
    "PIE",    # flake8-pie
    "PL",     # pylint
    "PT",     # flake8-pytest-style
    "PTH",    # flake8-pathlib
    "PYI",    # flake8-pyi
    "RET",    # flake8-return
    "RSE",    # flake8-raise
    "RUF",    # Ruff-specific rules (see `ignore` for exclusions)
    "S107",   # Possible hardcoded password assigned to function default
    "S102",   # Use of exec detected
    "S103",   # bad-file-permissions
    "S108",   # hardcoded-temp-file
    "S301",   # suspicious-pickle-usage
    "S306",   # suspicious-mktemp-usage
    "S307",   # suspicious-eval-usage
    "S313",   # suspicious-xmlc-element-tree-usage
    "S314",   # suspicious-xml-element-tree-usage
    "S315",   # suspicious-xml-expat-reader-usage
    "S316",   # suspicious-xml-expat-builder-usage
    "S317",   # suspicious-xml-sax-usage
    "S318",   # suspicious-xml-mini-dom-usage
    "S319",   # suspicious-xml-pull-dom-usage
    "S601",   # paramiko-call
    "S602",   # subprocess-popen-with-shell-equals-true
    "S604",   # call-with-shell-equals-true
    "S608",   # hardcoded-sql-expression
    "S609",   # unix-command-wildcard-injection
    "SIM",    # flake8-simplify
    "SLF",    # flake8-self
    "SLOT",   # flake8-slots
    "T100",   # Trace found: {name} used
    "T20",    # flake8-print
    "TC",     # flake8-type-checking
    "TID",    # Tidy imports
    "TRY",    # tryceratops
    "UP",     # pyupgrade
    "UP031",  # Use format specifiers instead of percent format
    "UP032",  # Use f-string instead of `format` call
    "W",      # pycodestyle
]

ignore = [
    "ASYNC109", # Async function definition with a `timeout` parameter Use `asyncio.timeout` instead
    "ASYNC110", # Use `asyncio.Event` instead of awaiting `asyncio.sleep` in a `while` loop
    "ASYNC240", # Use an async function for entering the file system
    "B008",     # Do not perform function call in argument defaults; commonly used in Home Assistant (e.g. cv.* validators)
    "B019",     # Use of functools.lru_cache or functools.cache on methods can lead to memory leaks
    "D202",     # No blank lines allowed after function docstring
    "D203",     # 1 blank line required before class docstring
    "D213",     # Multi-line docstring summary should start at the second line
    "D406",     # Section name should end with a newline
    "D407",     # Section name underlining
    "D417",     # Missing argument descriptions in docstring - to allow documenting only non-obvious parameters
    "E501",     # line too long

    "PLC1901", # {existing} can be simplified to {replacement} as an empty string is falsey; too many false positives
    "PLR0911", # Too many return statements ({returns} > {max_returns})
    "PLR0912", # Too many branches ({branches} > {max_branches})
    "PLR0913", # Too many arguments to function call ({c_args} > {max_args})
    "PLR0915", # Too many statements ({statements} > {max_statements})
    "PLR2004", # Magic value used in comparison, consider replacing {value} with a constant variable
    "PLW0108", # Unnecessary lambda wrapping a function call; can often be replaced by the function itself
    "PLW1641", # __eq__ without __hash__
    "PLW2901", # Outer {outer_kind} variable {name} overwritten by inner {inner_kind} target
    "PT011",   # pytest.raises({exception}) is too broad, set the `match` parameter or use a more specific exception
    "PT018",   # Assertion should be broken down into multiple parts
    "RUF001",  # String contains ambiguous unicode character.
    "RUF012",  # Mutable class attributes should be annotated with typing.ClassVar
    "RUF015",  # Prefer next(...) over single element slice
    "RUF043",  # Pattern passed to match= contains metacharacters but is neither escaped nor raw
    "SIM102",  # Use a single if statement instead of nested if statements
    "SIM103",  # Return the condition {condition} directly
    "SIM108",  # Use ternary operator {contents} instead of if-else-block
    "SIM115",  # Use context handler for opening files

    # Moving imports into type-checking blocks can mess with pytest.patch()
    "TC001", # Move application import {} into a type-checking block
    "TC002", # Move third-party import {} into a type-checking block
    "TC003", # Move standard library import {} into a type-checking block
    # Quotes for typing.cast generally not necessary, only for performance critical paths
    "TC006", # Add quotes to type expression in typing.cast()

    "TRY003", # Avoid specifying long messages outside the exception class
    "TRY400", # Use `logging.exception` instead of `logging.error`

    "UP047", # Non PEP 696 generic function
    "UP049", # Avoid private type parameter names

    # May conflict with the formatter, https://docs.astral.sh/ruff/formatter/#conflicting-lint-rules
    "W191",
    "E111",
    "E114",
    "E117",
    "D206",
    "D300",
    "Q",
    "COM812",
    "COM819",

    # Disabled because ruff does not understand type of __all__ generated by a function
    "PLE0605",

    "FURB116",
]

[lint.flake8-pytest-style]
fixture-parentheses = false
mark-parentheses = false

[lint.flake8-tidy-imports.banned-api]
"async_timeout".msg = "use asyncio.timeout instead"
"pytz".msg = "use zoneinfo instead"
"tests".msg = "You should not import tests"
"__future__.annotations".msg = "It should not be needed because Home Assistant requires Python 3.14+"

[lint.isort]
force-sort-within-sections = true
known-first-party = ["homeassistant"]
combine-as-imports = true
split-on-trailing-comma = false

[lint.per-file-ignores]
# Globs are matched relative to the config file's directory. The sync
# script copies this TOML into dist/ as ruff.toml, so these globs match
# the on-disk layout one directory below the config:
#
#   neopool/homeassistant/components/neopool/...
#   neopool/tests/components/neopool/...
#
# The leading `neopool/` segment keeps the lint helpers (ruff.toml,
# .ruff_cache) outside the core-shaped output (`dist/neopool/`) so
# that subtree contains only what's meant to land in a core checkout.

# Allow relative imports within the integration (TID252).
"neopool/homeassistant/components/*/*" = ["TID252"]
"neopool/tests/components/*/*" = ["TID252"]

# Temporary in core too — pathlib opt-in is a long migration; keep
# parity so we don't surface PTH violations the core repo also defers.
"neopool/homeassistant/**" = ["PTH"]

# `tests` is a first-party package in a real core checkout (its top-level
# tests/__init__.py marks it as such); see the isort comment above for
# why we declare the same here. The TID251 banned-api rule still fires
# in tests because `[lint.flake8-tidy-imports.banned-api]` lists
# `"tests"` explicitly — that ban is meant for *production* code, so we
# disable it inside the test tree (real core CI does the same via its
# tests/components/* per-file scope).
#
# SLF001 (private member access) is disabled in tests because several
# unit tests deliberately exercise private decoder helpers
# (_compute_ph_pump_status / _compute_*_polarity) and tweak private
# entity state (_debounce_delay, _last_pump_on) to keep the test
# instant and deterministic. Each occurrence has a comment in the
# test explaining why the public surface isn't enough.
"neopool/tests/**" = ["PTH", "TID251", "SLF001"]

[lint.mccabe]
max-complexity = 25

[lint.pydocstyle]
convention = "google"
property-decorators = ["propcache.api.cached_property"]
